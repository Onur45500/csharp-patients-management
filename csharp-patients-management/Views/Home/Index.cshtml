@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Manage patients</h1>
</div>

<button><a asp-controller="Home" asp-action="CreateOrEditPatient">Create</a></button>

@model List<Patient>

@* <table class="table">
    <thead>
        <th>Name</th>
        <th>Surname</th>
        <th>Sexe</th>
        <th>Height</th>
        <th>Weight</th>
        <th>Action</th>
    </thead>
    <tbody>
        @foreach(var patient in Model)
        {
            <tr>
                <td>@patient.Name</td>
                <td>@patient.Surname</td>
                <td>@patient.Sexe</td>
                <td>@patient.Height</td>
                <td>@patient.Weight</td>
                <td>
                    <a asp-controller="Home" asp-action="CreateOrEditPatient" asp-route-id="@patient.Id">Edit</a>
                    <a asp-controller="Home" asp-action="DeletePatient" asp-route-id="@patient.Id">Delete</a>
                </td>
            </tr>
        }
        
    </tbody>
</table> *@

<div class="card-container">
    @foreach(var patient in Model)
        {
    <div class="card">
        <img src="path/to/image1.jpg" alt="Card 1 Image">
        <div class="card-content">
            <h3>@patient.Name @patient.Surname</h3>
            <p>Sexe: @patient.Sexe Height: @patient.Height Weight: @patient.Weight</p>
            <p>
                <a asp-action="CreateOrEditPatient" asp-route-id="@patient.Id">Edit</a>
                <a asp-action="DeletePatient" asp-route-id="@patient.Id">Delete</a>
            </p>
        </div>
    </div>
        }
</div>