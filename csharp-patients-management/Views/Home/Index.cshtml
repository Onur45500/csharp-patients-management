@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Manage patients</h1>
</div>

<button><a asp-controller="Home" asp-action="CreateOrEditPatient">Create</a></button>

@model List<Patient>

@* <table class="table">
    <thead>
        <th>Name</th>
        <th>Surname</th>
        <th>Sexe</th>
        <th>Height</th>
        <th>Weight</th>
        <th>Action</th>
    </thead>
    <tbody>
        @foreach(var patient in Model)
        {
            <tr>
                <td>@patient.Name</td>
                <td>@patient.Surname</td>
                <td>@patient.Sexe</td>
                <td>@patient.Height</td>
                <td>@patient.Weight</td>
                <td>
                    <a asp-controller="Home" asp-action="CreateOrEditPatient" asp-route-id="@patient.Id">Edit</a>
                    <a asp-controller="Home" asp-action="DeletePatient" asp-route-id="@patient.Id">Delete</a>
                </td>
            </tr>
        }
        
    </tbody>
</table> *@

<div class="row">
    @foreach(var patient in Model)
        {
    <div class="col-sm-3 m-2">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@patient.Name @patient.Surname</h5>
                <h6 class="card-title">Sexe: @patient.Sexe</h6>
                <h6 class="card-title">Height: @patient.Height</h6>
                <h6 class="card-title">Weight: @patient.Weight</h6>
                <p>@* <a asp-action="CreateOrEditPatient" asp-route-id="@patient.Id">Edit</a> *@
                <a asp-action="DeletePatient" asp-route-id="@patient.Id">Delete</a></p>
            </div>
        </div>
    </div>
        }
</div>